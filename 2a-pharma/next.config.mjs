/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  images: { unoptimized: true },

  basePath: "/2apharma",
  assetPrefix: "/2apharma/",
};

export default nextConfig;